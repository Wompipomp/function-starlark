version: "2"

run:
  go: "1.25"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - gocritic
    - misspell

formatters:
  enable:
    - goimports

issues:
  exclude-generated: strict
