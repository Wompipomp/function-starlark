apiVersion: e2e.fn-starlark.io/v1
kind: XTest
metadata:
  name: test-mutable-struct
  labels:
    e2e-test: mutable-struct
spec:
  compositionRef:
    name: mutable-struct-test
  region: us-east-1
  environment: prod
